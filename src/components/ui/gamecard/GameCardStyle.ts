import styled from 'styled-components';
import { GameCardBox } from '../../../common/Style';

export const FixedGameCardBox = styled(GameCardBox)`
  height: 150px;
`;

export const ControlBox = styled.div`
  display: flex;
  margin: 0 2rem 1rem;
`;

export const StatusBox = styled.div`
  margin: 1rem;
  text-align: center;
  color: grey;
`;
